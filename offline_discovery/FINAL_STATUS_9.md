# Verified result

Recovered hidden compositional law from local observations with network isolation enabled.

`y = 5 + x*t + (2*t - 3*x)`

Unseen outputs exactly matched: `11, 7, -37, 0`.

Maximum error: `0`.
